# ===================================
# INTRADAY TRADING BOT DEPENDENCIES
# ===================================
# These cover everything except live Angel One trading: backtesting, the
# research scripts, paper trading, the dashboard, and the Dhan and Yahoo data
# feeds. For live trading through Angel One, also install:
#
#     pip install -r requirements-angel.txt

# Market Data
yfinance>=0.2.40              # Yahoo feed (no account needed)
# The Dhan feed uses the REST API directly and needs no SDK.

# Data Processing & Analysis
pandas>=2.0.0
numpy>=1.24.0
ta>=0.11.0                    # Technical Analysis (RSI, VWAP, etc.)

# API Server (for Dashboard)
flask>=3.0.0
flask-cors>=4.0.0
PyJWT>=2.8.0                  # JWT authentication

# Scheduling
schedule>=1.2.0
APScheduler>=3.10.4
python-dateutil>=2.8.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
loguru>=0.7.0
